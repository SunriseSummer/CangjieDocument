# 安装仓颉工具链

仓颉为开发者提供了一整套开箱即用的开发工具链，包括编译器、调试器、项目管理工具等。本文档将指导您如何在 Linux、macOS 和 Windows 平台上安装和配置仓颉工具链。

## 1. 下载工具链

请前往仓颉编程语言官方网站下载适配您操作系统的安装包：

- **Linux**: `cangjie-sdk-linux-x64-x.y.z.tar.gz` (x86_64) 或 `cangjie-sdk-linux-aarch64-x.y.z.tar.gz` (aarch64)
- **macOS**: `cangjie-sdk-mac-x64-x.y.z.tar.gz` (x86_64) 或 `cangjie-sdk-mac-aarch64-x.y.z.tar.gz` (Apple Silicon)
- **Windows**: `cangjie-sdk-windows-x64-x.y.z.exe` (安装程序) 或 `cangjie-sdk-windows-x64-x.y.z.zip` (压缩包)

---

## 2. 安装步骤

### 🐧 Linux

#### 环境准备
根据架构不同，您需要满足以下环境要求：
- **x86_64 / aarch64**: glibc 2.27+, Linux Kernel 4.15+, libstdc++ 6.0.24+
- **Ubuntu 18.04 额外依赖**: `sudo apt-get install binutils libc-dev libc++-dev libgcc-7-dev`

> ⚠️ **注意**: 仓颉工具链依赖 OpenSSL 3。如果系统未安装且源中没有，请参考[附录](../Appendix/linux_toolchain_install.md)手动安装。

#### 安装与配置
1.  **解压安装包**:
    ```bash
    tar xvf cangjie-sdk-linux-x64-x.y.z.tar.gz
    ```
    解压后会生成 `cangjie` 目录。

2.  **配置环境变量**:
    要立即生效，请在终端执行：
    ```bash
    source cangjie/envsetup.sh
    ```

    要**永久生效**，请将 `source /path/to/cangjie/envsetup.sh` 添加到您的 shell 配置文件（如 `~/.bashrc` 或 `~/.zshrc`）末尾。

3.  **验证安装**:
    ```bash
    cjc -v
    ```
    如果输出了版本信息，即表示安装成功。

---

### 🍎 macOS

#### 环境准备
- macOS 12.0 及以上版本。
- 安装依赖: `brew install libffi`

#### 安装与配置
步骤与 Linux 相同：
1.  **解压**: `tar xvf cangjie-sdk-mac-x64-x.y.z.tar.gz`
2.  **配置**: `source cangjie/envsetup.sh` (或添加到 `~/.zshrc`)
3.  **验证**: `cjc -v`

---

### 🪟 Windows

#### 方法一：使用安装程序 (推荐)
直接运行下载的 `.exe` 安装包（如 `cangjie-sdk-windows-x64-x.y.z.exe`），跟随安装向导即可完成安装和环境配置。

#### 方法二：使用压缩包 (Zip)
1.  **解压**: 将压缩包解压到指定目录（例如 `D:\cangjie`）。

2.  **配置环境变量**:
    *   **临时生效**:
        *   CMD: 运行 `D:\cangjie\envsetup.bat`
        *   PowerShell: 运行 `. D:\cangjie\envsetup.ps1`

    *   **永久生效**:
        1. 打开 Windows 搜索，输入“编辑系统环境变量”。
        2. 点击“环境变量”。
        3. **新建用户变量** `CANGJIE_HOME`，值为解压路径（如 `D:\cangjie`）。
        4. **编辑用户变量** `Path`，添加以下条目：
           - `%CANGJIE_HOME%\bin`
           - `%CANGJIE_HOME%\tools\bin`
           - `%CANGJIE_HOME%\tools\lib`
           - `%CANGJIE_HOME%\runtime\lib\windows_x86_64_cjnative`
           - `%USERPROFILE%\.cjpm\bin` (可选，用于 cjpm 工具)
        5. 确认保存并重启终端。

3.  **验证安装**:
    打开新的终端窗口，执行 `cjc -v`。

---

## 3. 卸载与更新

- **卸载**: 直接删除安装目录（如 `rm -rf cangjie`），并清理相应的环境变量配置。如果是 Windows exe 安装，可使用控制面板或 `unins000.exe` 卸载。
- **更新**: 建议先卸载旧版本，再安装新版本。

> 更多 Linux 发行版支持详情，请参阅[附录](../Appendix/linux_toolchain_install.md)。
